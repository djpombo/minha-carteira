import styled from "styled-components";

export const Container = styled.div`
  
    > select {
        padding: .4rem .7rem;
        border-radius: .4rem;
        margin-left: .7rem;
               
        }

`;